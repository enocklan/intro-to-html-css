function addNumbers(num1, num2) { // Function definition with parameters

  let sum = num1 + num2; // Local variable 'sum' stores the result

  return sum; // Return the calculated sum

}

let result = addNumbers(5, 3); // Function call with arguments (5, 3)


console.log("The sum is:", result); // Use the returned value (8)
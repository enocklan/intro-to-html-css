let studentNames = ["John", "Jane", "Mark"]
let indexZero = studentNames[0]
studentNames.push("Waleed")
studentNames.push("Duncan")
let studentNamesLength = studentNames.length
let lastItem= studentNames[studentNames.lenght-1]

// console.log(lastItem)

// let arrayItems =["john", 1 ,]

// console.log(arrayItems[arrayItems.length-1])
// objects
// objects
let car = {
  model: "Mercedes",
  yom: 2024,
  color: "Black",
  millage:null
}
carModel = car["model"]
carColor = car.color
carMillage=car.millage
// console.log([carModel, carColor, carMillage])
// check the datatype using typeof keyword

console.log(typeof car.millage)